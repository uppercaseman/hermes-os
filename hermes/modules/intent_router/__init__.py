"""Hermes Intent Router: a generic, provider-independent request router
that implements Commander's own `IntentClassifier` and `WorkflowResolver`
protocols from one shared routing table. See interface.py and README.md.
"""
