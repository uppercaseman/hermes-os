"""Vertical-slice demos: end-to-end wiring of real Hermes modules,
proving the pipeline works together rather than adding new architecture.
Nothing under here is a core module -- it's integration glue plus one
fake/mock adapter per demo, so a demo can run with no live external API.
"""
